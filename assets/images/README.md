# Images

Put local preview images here only if they are approved for public display.

For production, prefer adding Google Drive image IDs to `QEF_Photos` or folder IDs to `QEF_Pages` in the Google Sheet backend.
